# RQ3: Within-Repository Outcomes

Owner: Luke Thomas

RQ3 compares agents within repositories where multiple agents appear, holding repository context more constant than global agent comparisons. It covers acceptance, merge speed, and specialization by task type or change size.

## Numbering Note

Luke's submitted package used `Q4` labels during drafting. In the final report, this work maps to RQ3: within-repository outcomes.

## Scripts

- `scripts/run_q4_analysis.py`: full within-repository analysis pipeline.
- `scripts/checkpoint_feasibility.py`: feasibility check for the k=5 and k=10 thresholds.
- `scripts/SENG404_Q4_starter.ipynb`: original executed notebook from the teammate package.

## Results

The committed `results/` folder includes Luke's submitted results summary and figures.

## Data

Place required AIDev parquet files in `RQ3/data/` before rerunning the scripts.
